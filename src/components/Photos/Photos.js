import React from "react";
import {PhotosWrapper} from "../StyledComponents/StyledComponents";
export default class Photos extends React.Component{
    render(){
        return(
            <PhotosWrapper>
                photos
            </PhotosWrapper>
        )
    }
}