import React from "react";
import {PostsWrapper} from "../StyledComponents/StyledComponents";

export default class Posts extends React.Component{
    render(){
        return(
            <PostsWrapper>Posts</PostsWrapper>
        )
    }
}