import React from "react";
import {ContactsWrapper} from "../StyledComponents/StyledComponents";
export default class Contacts extends React.Component{
    render(){
        return(
            <ContactsWrapper>Contacts</ContactsWrapper>
        )
    }
}